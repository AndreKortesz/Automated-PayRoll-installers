# Развёртывание (Deployment)

## Оглавление

1. [Требования](#требования)
2. [Локальная разработка](#локальная-разработка)
3. [Railway](#railway)
4. [Переменные окружения](#переменные-окружения)
5. [Bitrix24 OAuth](#bitrix24-oauth)
6. [База данных](#база-данных)
7. [Мониторинг](#мониторинг)

---

## Требования

### Системные

- Python 3.11+
- PostgreSQL 14+
- Docker (опционально)

### Python зависимости

```txt
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
jinja2==3.1.2
sqlalchemy==2.0.23
databases==0.8.0
asyncpg==0.29.0
pandas==2.1.3
openpyxl==3.1.2
httpx==0.25.2
aiohttp==3.9.1
python-jose==3.3.0
```

---

## Локальная разработка

### 1. Клонирование репозитория

```bash
git clone https://github.com/AndreKortesz/Automated-PayRoll-installers.git
cd Automated-PayRoll-installers
```

### 2. Виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Переменные окружения

Создайте файл `.env`:

```env
DATABASE_URL=postgresql://localhost:5432/salary_service
BITRIX_CLIENT_ID=your_client_id
BITRIX_CLIENT_SECRET=your_client_secret
BITRIX_DOMAIN=svyaz.bitrix24.ru
YANDEX_GEOCODER_API_KEY=your_api_key
```

### 5. База данных

```bash
# Создание базы
createdb salary_service

# Таблицы создаются автоматически при запуске
```

### 6. Запуск

```bash
uvicorn backend.app:app --reload --host 0.0.0.0 --port 8000
```

Приложение доступно по адресу: http://localhost:8000

---

## Railway

### Структура проекта

```
├── backend/
│   ├── app.py
│   ├── ...
├── frontend/
│   └── templates/
├── Dockerfile
├── requirements.txt
└── railway.json (опционально)
```

### Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Установка зависимостей
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копирование кода
COPY . .

# Запуск
CMD ["uvicorn", "backend.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Настройка Railway

1. **Создайте проект** в Railway
2. **Подключите GitHub** репозиторий
3. **Добавьте PostgreSQL** сервис
4. **Настройте переменные** окружения
5. **Деплой** происходит автоматически при push в main

### Переменные в Railway

```
DATABASE_URL         → Автоматически от PostgreSQL
BITRIX_CLIENT_ID     → Из приложения Bitrix24
BITRIX_CLIENT_SECRET → Из приложения Bitrix24
BITRIX_DOMAIN        → svyaz.bitrix24.ru
YANDEX_GEOCODER_API_KEY → Из кабинета Яндекс
```

### Домен

Railway автоматически предоставляет домен:
```
https://automated-payroll-installers-production.up.railway.app
```

---

## Переменные окружения

### Обязательные

| Переменная | Описание | Пример |
|------------|----------|--------|
| DATABASE_URL | Строка подключения PostgreSQL | postgresql://user:pass@host:5432/db |
| BITRIX_CLIENT_ID | ID приложения Bitrix24 | local.xxxxxxxx.xxxxxxxx |
| BITRIX_CLIENT_SECRET | Секрет приложения | xxxxxxxxxxxxxxxxxxxxxxxx |
| BITRIX_DOMAIN | Домен Bitrix24 | svyaz.bitrix24.ru |
| YANDEX_GEOCODER_API_KEY | API ключ Яндекс Геокодера | xxxxxxxx-xxxx-xxxx-xxxx |

### Опциональные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| PORT | Порт приложения | 8000 |
| DEBUG | Режим отладки | false |
| SESSION_SECRET | Секрет для сессий | auto-generated |

---

## Bitrix24 OAuth

### Создание приложения

1. Войдите в Bitrix24 как администратор
2. Перейдите в **Разработчикам → Интеграции → Добавить**
3. Выберите **Локальное приложение**
4. Заполните:
   - Название: "Salary Service"
   - Обработчик: `https://your-domain.railway.app/`
   - URL для установки: `https://your-domain.railway.app/auth/callback`

### Необходимые права

- `user` — информация о пользователе
- `im` — уведомления (опционально)

### Callback URL

```
https://automated-payroll-installers-production.up.railway.app/auth/callback
```

---

## База данных

### Схема

Таблицы создаются автоматически при первом запуске:

```python
# backend/database.py
async def create_tables():
    """Создаёт таблицы если не существуют"""
    ...
```

### Миграции

Миграции выполняются в `run_migrations()`:

```python
async def run_migrations():
    """Применяет миграции к существующей БД"""
    migrations = [
        "ALTER TABLE uploads ADD COLUMN IF NOT EXISTS created_by INTEGER",
        "ALTER TABLE periods ADD COLUMN IF NOT EXISTS status VARCHAR(20)",
        ...
    ]
```

### Резервное копирование

Railway автоматически делает бэкапы PostgreSQL.

Ручной бэкап:
```bash
pg_dump $DATABASE_URL > backup.sql
```

Восстановление:
```bash
psql $DATABASE_URL < backup.sql
```

---

## Мониторинг

### Логи

Railway предоставляет логи в реальном времени:
- Deploy Logs — процесс сборки
- HTTP Logs — HTTP запросы
- View Logs — логи приложения

### Ключевые логи приложения

```
✅ Connected to PostgreSQL          — БД подключена
✅ Tables created                   — Таблицы созданы
✅ User logged in: Name (ID: X)     — Пользователь авторизован
📊 Found version X with Y orders    — Найдена версия для сравнения
📋 Total extra_rows found: X        — Найдены ручные корректировки
⛽ Бензин: адрес -> X км -> Y руб   — Расчёт бензина
```

### Ошибки для мониторинга

```
⚠️ Changes comparison error         — Ошибка сравнения версий
❌ Database connection failed       — Нет подключения к БД
🔐 Token exchange failed            — Ошибка OAuth
```

### Healthcheck

```http
GET /
```

Если приложение работает — редирект на /login или главную страницу.

---

## Troubleshooting

### Ошибка Docker Hub

```
ERROR: failed to fetch oauth token: 500 Internal Server Error
```

**Решение**: Подождите 5-10 минут и нажмите Redeploy.

### Ошибка подключения к БД

```
❌ Database connection failed
```

**Проверьте**:
1. DATABASE_URL в переменных окружения
2. PostgreSQL сервис запущен
3. Firewall разрешает подключение

### OAuth ошибка

```
🔐 Token exchange failed
```

**Проверьте**:
1. BITRIX_CLIENT_ID и BITRIX_CLIENT_SECRET
2. Callback URL в настройках приложения Bitrix24
3. Домен совпадает с BITRIX_DOMAIN

### Медленный расчёт бензина

Yandex Geocoder имеет лимиты. Результаты кэшируются в памяти.

**Если всё медленно**: перезапустите приложение для очистки кэша.
