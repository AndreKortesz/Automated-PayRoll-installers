# 🧮 Правила расчёта зарплат — Детальное описание

## Содержание
1. [Общая формула](#общая-формула)
2. [Оплата услуг](#1-оплата-услуг)
3. [Расчёт бензина](#2-расчёт-бензина)
4. [Расчёт транспортных](#3-расчёт-транспортных)
5. [Яндекс Заправки](#4-яндекс-заправки)
6. [Диагностика -50%](#5-диагностика--50)
7. [Заказы с оплатой клиентом](#6-заказы-с-оплатой-клиентом)
8. [Ручные корректировки](#7-ручные-корректировки)
9. [Примеры расчётов](#8-примеры-расчётов)
10. [Edge cases](#9-edge-cases)

---

## Общая формула

### Итого по заказу

```
┌────────────────────────────────────────────────────────────┐
│  ИТОГО_заказа = Оплата_услуг + Бензин + Транспортные       │
└────────────────────────────────────────────────────────────┘
```

### Итого по монтажнику

```
┌────────────────────────────────────────────────────────────────────────┐
│  ИТОГО_монтажника = Σ(ИТОГО_заказов) - Диагностика_50% - Яндекс_90%   │
└────────────────────────────────────────────────────────────────────────┘
```

### Визуальная схема

```
Входные данные из 1С:
├── Выручка от услуг ─────────────┐
├── Процент (30%, 20%, 40%) ──────┼──► Оплата услуг
│                                 │
├── Адрес ────────────────────────┼──► Геокодирование ──► Бензин
├── Выезд специалиста ────────────┘
│
├── Выручка > 10000? ─────────────┐
├── Процент 20-40%? ──────────────┼──► Транспортные
├── Не на служ. авто? ────────────┘
│
├── Оплата клиентом? ─────────────────► Диагностика -50%
│
└── Яндекс Заправки (16-31) ──────────► Вычет 90%

                                        ▼
                               ┌────────────────┐
                               │ ИТОГО К ВЫПЛАТЕ│
                               └────────────────┘
```

---

## 1. Оплата услуг

### Формула

```python
Оплата_услуг = Выручка_от_услуг × (Процент / 100)
```

### Исходные данные

| Поле в 1С | Поле в БД | Описание |
|-----------|-----------|----------|
| Выручка от услуг | `revenue_services` | Сумма за работу |
| % | `percent` | Процент монтажника |

### Типичные проценты

| Процент | Описание | Когда применяется |
|---------|----------|-------------------|
| **30%** | Стандартный | Большинство заказов |
| **20%** | Пониженный | Крупные заказы |
| **40%** | Повышенный | Сложные работы |
| **50%** | Специальный | Диагностика |
| **100%** | Полный | Премии, доплаты |

### Особые случаи

#### Выезд специалиста > 0
```python
if specialist_fee > 0:
    # Монтажник получает только выезд, а не процент
    # Но в оплату услуг это не влияет напрямую
    pass
```

#### Процент = 0%
```python
if percent == "0%":
    # Скорее всего ошибка в данных 1С
    # Нужно проверить вручную
    service_payment = 0
```

### Пример

```
Выручка от услуг: 50 000 ₽
Процент: 30%

Оплата услуг = 50 000 × 0.30 = 15 000 ₽
```

---

## 2. Расчёт бензина

### Условия начисления

```python
def should_calculate_fuel(order: dict) -> bool:
    # ❌ Выезд специалиста > 0 (монтажник не ехал сам)
    if order["specialist_fee"] > 0:
        return False
    
    # ❌ Заказ с оплатой клиентом
    if order["is_client_payment"]:
        return False
    
    # ❌ Адрес не указан
    if not order["address"]:
        return False
    
    # ✅ Бензин начисляется
    return True
```

### Алгоритм расчёта

```
1. Получить адрес из заказа
2. Очистить адрес (убрать лишнее)
3. Отправить в Yandex Geocoder
4. Получить координаты (lat, lon)
5. Рассчитать расстояние до офиса (Haversine)
6. Применить тарифную сетку
```

### Координаты офиса

```python
OFFICE_COORDS = (55.8309, 37.4294)
# Адрес: Москва, Сходненский тупик, 16с4
```

### Тарифная сетка

```python
FUEL_TARIFFS = [
    (10, 100),     # 0-10 км → 100 ₽
    (20, 200),     # 10-20 км → 200 ₽
    (30, 300),     # 20-30 км → 300 ₽
    (40, 400),     # 30-40 км → 400 ₽
    (50, 500),     # 40-50 км → 500 ₽
    (60, 600),     # 50-60 км → 600 ₽
    (80, 900),     # 60-80 км → 900 ₽
    (100, 1200),   # 80-100 км → 1 200 ₽
    (150, 1700),   # 100-150 км → 1 700 ₽
    (200, 2200),   # 150-200 км → 2 200 ₽
    (999, 3000),   # 200+ км → 3 000 ₽
]
```

### Функция расчёта

```python
def calculate_fuel_cost(distance_km: float) -> float:
    for max_distance, payment in FUEL_TARIFFS:
        if distance_km <= max_distance:
            return payment
    return FUEL_TARIFFS[-1][1]  # 3000₽ для любого > 200км
```

### Пример

```
Адрес: "г. Москва, ул. Тверская, 1"
Координаты: (55.7558, 37.6173)
Расстояние до офиса: 15.2 км

Бензин = 200 ₽ (попадает в диапазон 10-20 км)
```

### Кэширование

```python
# Результаты геокодирования кэшируются
distance_cache = {}

async def get_distance_cached(address: str) -> float:
    if address in distance_cache:
        return distance_cache[address]
    
    coords = await geocode_address_yandex(address)
    distance = calculate_distance(coords)
    
    distance_cache[address] = distance
    return distance
```

---

## 3. Расчёт транспортных

### Условия начисления

Транспортные (1 000 ₽) начисляются если **ВСЕ** условия выполняются:

```python
def should_calculate_transport(order: dict, worker: str) -> bool:
    # ✅ Условие 1: Выручка от услуг > 10 000 ₽
    if order["revenue_services"] <= 10000:
        return False
    
    # ✅ Условие 2: Процент от 20% до 40%
    percent = parse_percent(order["percent"])
    if percent < 20 or percent > 40:
        return False
    
    # ✅ Условие 3: Монтажник НЕ на служебном авто
    if worker in COMPANY_CAR_WORKERS:
        return False
    
    # ✅ Условие 4: Не заказ с оплатой клиентом
    if order["is_client_payment"]:
        return False
    
    return True
```

### Список монтажников на служебном авто

```python
COMPANY_CAR_WORKERS = [
    "Ветренко Дмитрий Андреевич",
    # ... другие
]
```

**Важно:** Этот список нужно обновлять при изменениях в компании.

### Конфигурация

```python
DEFAULT_CONFIG = {
    "transport_min_revenue": 10000,  # Минимальная выручка
    "transport_percent_min": 20,     # Минимальный процент
    "transport_percent_max": 40,     # Максимальный процент
    "transport_amount": 1000,        # Сумма транспортных
}
```

### Примеры

| Выручка | Процент | Служ. авто | Транспорт |
|---------|---------|------------|-----------|
| 15 000 ₽ | 30% | Нет | ✅ 1 000 ₽ |
| 8 000 ₽ | 30% | Нет | ❌ 0 ₽ (выручка < 10000) |
| 50 000 ₽ | 50% | Нет | ❌ 0 ₽ (процент > 40) |
| 20 000 ₽ | 30% | Да | ❌ 0 ₽ (служ. авто) |

---

## 4. Яндекс Заправки

### Описание

Компания оплачивает топливо сотрудникам через сервис Яндекс Заправки. В конце месяца сумма заправок вычитается из зарплаты **со скидкой 10%**.

### Условия применения

```python
def should_apply_yandex_fuel(period_name: str) -> bool:
    # Только для второй половины месяца (16-30/31)
    start_day = int(period_name.split("-")[0])
    return start_day >= 16
```

| Период | Яндекс Заправки |
|--------|-----------------|
| 01-15.11.25 | ❌ Не применяется |
| 16-30.11.25 | ✅ Применяется |
| 16-31.12.25 | ✅ Применяется |
| 16-28.02.25 | ✅ Применяется (февраль) |

### Формула вычета

```python
Вычет = Сумма_заправок × 0.9
```

Монтажник **экономит 10%** на топливе.

### Пример

```
Монтажник: Иванов Иван
Сумма заправок за месяц: 10 000 ₽

Вычет = 10 000 × 0.9 = 9 000 ₽

Экономия монтажника: 1 000 ₽
```

### Парсинг файла

Файл Яндекс Заправки — Excel с колонками:
- ФИО сотрудника
- Сумма

```python
def parse_yandex_fuel(file: BytesIO) -> dict:
    """
    Возвращает: {
        "Иванов Иван": 10000,
        "Петров Пётр": 8500
    }
    """
```

### Сопоставление имён

Имена в файле Яндекс Заправки могут отличаться от имён в 1С:
- "Иванов Иван Иванович" → "Иванов Иван"
- "Петров П.П." → "Петров Пётр"

Система нормализует имена для сопоставления.

---

## 5. Диагностика -50%

### Описание

Для заказов **с оплатой клиентом** диагностика оплачивается по ставке 50%.

### Формула

```python
Диагностика_50 = Оплата_диагностики × 0.5
```

### Когда применяется

```python
def calculate_diagnostic_50(order: dict) -> float:
    if not order["is_client_payment"]:
        return 0
    
    diagnostic_payment = order.get("diagnostic_payment", 0)
    return diagnostic_payment * 0.5
```

### Пример

```
Заказ: КАУТ-001234 (оплата клиентом)
Оплата диагностики: 2 000 ₽

Диагностика -50% = 2 000 × 0.5 = 1 000 ₽

Эта сумма ВЫЧИТАЕТСЯ из итого монтажника
```

---

## 6. Заказы с оплатой клиентом

### Определение

Заказ считается "с оплатой клиентом" если:

```python
def is_client_payment(worker_name: str) -> bool:
    return "(оплата клиентом)" in worker_name.lower()
```

### Особенности расчёта

| Параметр | Обычный заказ | С оплатой клиентом |
|----------|---------------|-------------------|
| Оплата услуг | ✅ Полная | ✅ Полная |
| Бензин | ✅ Рассчитывается | ❌ Не начисляется |
| Транспортные | ✅ При условиях | ❌ Не начисляются |
| Диагностика | 100% | 50% |

### Отображение в отчёте

Заказы с оплатой клиентом группируются отдельно:

```
Иванов Иван
├── КАУТ-001234 (компания) ─── 15 000 ₽
├── КАУТ-001235 (компания) ─── 12 000 ₽
└── ИТОГО компания: 27 000 ₽

Иванов Иван (оплата клиентом)
├── КАУТ-001236 (клиент) ───── 5 000 ₽
└── ИТОГО клиент: 5 000 ₽

ИТОГО Иванов: 32 000 ₽
```

---

## 7. Ручные корректировки

### Типы корректировок

1. **Доплаты** — положительные суммы
   - Премии
   - Отпускные
   - Компенсации

2. **Вычеты** — отрицательные суммы
   - Штрафы
   - Ремонт авто
   - Возмещение ущерба

### Добавление корректировки

```python
# Через UI: кнопка "+ Добавить строку"
# Создаётся order с is_extra_row = True

{
    "worker": "Иванов Иван Иванович",
    "order_code": "-",
    "address": "Доплата за Кардан",  # Описание
    "service_payment": 5000,         # Сумма (+ или -)
    "is_extra_row": True
}
```

### Сохранение при перезагрузке

При загрузке новых файлов 1С:
1. Система находит `is_extra_row = True` из предыдущей версии
2. Показывает их в секции "Удалённые / Ручные корректировки"
3. Пользователь выбирает какие восстановить
4. Выбранные переносятся в новую версию

---

## 8. Примеры расчётов

### Пример 1: Стандартный заказ

**Входные данные:**
```
Монтажник: Иванов Иван
Заказ: КАУТ-001234
Адрес: г. Москва, ул. Ленина, 1 (25 км от офиса)
Выручка от услуг: 50 000 ₽
Процент: 30%
Выезд специалиста: 0
```

**Расчёт:**
```
Оплата услуг = 50 000 × 0.30 = 15 000 ₽
Бензин = 300 ₽ (25 км → диапазон 20-30 км)
Транспортные = 1 000 ₽ (выручка > 10000, процент 30%)

ИТОГО = 15 000 + 300 + 1 000 = 16 300 ₽
```

### Пример 2: Заказ с выездом специалиста

**Входные данные:**
```
Монтажник: Петров Пётр
Заказ: КАУТ-001235
Адрес: г. Москва, ул. Тверская, 1
Выручка от услуг: 30 000 ₽
Процент: 30%
Выезд специалиста: 1 500 ₽
```

**Расчёт:**
```
Оплата услуг = 30 000 × 0.30 = 9 000 ₽
Бензин = 0 ₽ (есть выезд специалиста!)
Транспортные = 1 000 ₽ (выручка > 10000)

ИТОГО = 9 000 + 0 + 1 000 = 10 000 ₽
```

### Пример 3: Заказ с оплатой клиентом

**Входные данные:**
```
Монтажник: Иванов Иван (оплата клиентом)
Заказ: КАУТ-001236
Адрес: г. Москва, ул. Арбат, 1
Выручка от услуг: 20 000 ₽
Процент: 30%
Оплата диагностики: 2 000 ₽
```

**Расчёт:**
```
Оплата услуг = 20 000 × 0.30 = 6 000 ₽
Бензин = 0 ₽ (оплата клиентом!)
Транспортные = 0 ₽ (оплата клиентом!)
Диагностика -50% = 2 000 × 0.5 = 1 000 ₽ (вычитается)

ИТОГО = 6 000 + 0 + 0 - 1 000 = 5 000 ₽
```

### Пример 4: Итого по монтажнику за период

**Заказы:**
```
1. КАУТ-001234 → 16 300 ₽
2. КАУТ-001235 → 10 000 ₽
3. КАУТ-001236 (клиент) → 5 000 ₽
```

**Яндекс Заправки (период 16-30):**
```
Сумма заправок: 8 000 ₽
Вычет = 8 000 × 0.9 = 7 200 ₽
```

**Итого:**
```
Сумма заказов = 16 300 + 10 000 + 5 000 = 31 300 ₽
Минус Яндекс = 31 300 - 7 200 = 24 100 ₽

ИТОГО К ВЫПЛАТЕ: 24 100 ₽
```

---

## 9. Edge cases

### Адрес не распознан

```python
if coords is None:
    # Геокодер не нашёл адрес
    fuel_payment = 0
    # Можно установить вручную
```

### Расстояние = 0

```python
if distance_km == 0:
    # Адрес совпадает с офисом (?)
    fuel_payment = 0
```

### Дубликаты заказов

```python
# Один заказ может быть у разных монтажников
# Например, работали вдвоём
# Система сохраняет оба
```

### Отрицательная сумма

```python
# Возможно при ручных корректировках
if total < 0:
    # Допустимо (вычет больше начислений)
    pass
```

### Февраль (16-28 или 16-29)

```python
def get_february_end(year: int) -> int:
    """Високосный год?"""
    if year % 4 == 0 and (year % 100 != 0 or year % 400 == 0):
        return 29
    return 28
```

### Монтажник без заказов

```python
# Если у монтажника только Яндекс Заправки
# Итого может быть отрицательным
# Это нормально — вычет за прошлый месяц
```
