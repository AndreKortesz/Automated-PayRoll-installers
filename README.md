# 🧮 Salary Service — Автоматизированный расчёт зарплат монтажников

## 📋 Оглавление

1. [Обзор системы](#-обзор-системы)
2. [Для кого этот сервис](#-для-кого-этот-сервис)
3. [Ключевые возможности](#-ключевые-возможности)
4. [Архитектура проекта](#-архитектура-проекта)
5. [Бизнес-логика расчётов](#-бизнес-логика-расчётов)
6. [Workflow статусов](#-workflow-статусов)
7. [Роли и права доступа](#-роли-и-права-доступа)
8. [База данных](#-база-данных)
9. [API Reference](#-api-reference)
10. [Фронтенд и UI](#-фронтенд-и-ui)
11. [Интеграции](#-интеграции)
12. [Развёртывание](#-развёртывание)
13. [Конфигурация](#-конфигурация)
14. [Troubleshooting](#-troubleshooting)

---

## 🎯 Обзор системы

**Salary Service** — веб-приложение для автоматического расчёта зарплат монтажников компании **Mos-GSM** на основе данных из системы 1С.

### Проблема, которую решает

Ежемесячно бухгалтерия тратит часы на:
- Ручной расчёт зарплат 20+ монтажников
- Определение оплаты за бензин по расстоянию до объекта
- Учёт транспортных расходов
- Вычет Яндекс Заправок
- Формирование отчётов для каждого монтажника

**Salary Service автоматизирует весь этот процесс**: загрузил 2 файла из 1С → получил готовые Excel-отчёты для бухгалтерии и монтажников.

### Технологический стек

| Компонент | Технология |
|-----------|------------|
| Backend | Python 3.11, FastAPI, SQLAlchemy |
| Database | PostgreSQL |
| Frontend | HTML/CSS/JavaScript (vanilla), Jinja2 templates |
| Auth | OAuth2 через Bitrix24 |
| Геокодирование | Yandex Geocoder API |
| Hosting | Railway |
| Excel | openpyxl |

---

## 👥 Для кого этот сервис

### Пользователи системы

1. **Администратор** — полный доступ, управление периодами, пользователями
2. **Менеджер** — загрузка файлов, редактирование данных, отправка расчётов
3. **Viewer** — только просмотр данных (для монтажников)

### Типичный сценарий использования

```
1. Бухгалтер выгружает 2 файла из 1С (выручка + диагностика)
2. Загружает их в Salary Service
3. Система автоматически рассчитывает:
   - Оплату за услуги (выручка × процент)
   - Бензин (по расстоянию от офиса до адреса)
   - Транспортные (1000₽ при выручке > 10000₽)
   - Вычет Яндекс Заправок (для 16-31 числа)
4. Бухгалтер проверяет, при необходимости корректирует
5. Скачивает готовые Excel-отчёты
6. Отправляет монтажникам через Bitrix24
```

---

## ✨ Ключевые возможности

### 📊 Автоматический расчёт

- **Оплата услуг** = Выручка × Процент (из файла 1С)
- **Бензин** = f(расстояние) через Yandex Geocoder
- **Транспортные** = 1000₽ при условиях
- **Диагностика -50%** для заказов с оплатой клиентом
- **Яндекс Заправки** — вычет 90% суммы заправок

### 📁 Работа с файлами

- Автоопределение типа файла (выручка/диагностика) по строке "Отбор"
- Извлечение названия периода из заголовка файла
- Парсинг Excel с корректной обработкой объединённых ячеек
- Drag & drop загрузка файлов

### 🔄 Версионирование

- Каждая загрузка создаёт новую версию
- Сравнение с предыдущей версией
- Восстановление ручных корректировок
- История изменений

### 📤 Генерация отчётов

- **Бухгалтерский отчёт** — сводная таблица по всем монтажникам
- **Индивидуальные отчёты** — для каждого монтажника отдельный файл
- **ZIP-архив** — все отчёты в одном архиве

### 🔍 Глобальный поиск

- Поиск по номеру заказа, адресу, ФИО монтажника
- Fuzzy-поиск с учётом опечаток (Аголаров → Агаларов)
- Автозамена ё↔е (Озёрная ↔ Озерная)
- Autocomplete dropdown с результатами

### 📱 Интеграция с Bitrix24

- OAuth2 авторизация
- Отправка сообщений монтажникам
- Получение списка сотрудников

---

## 🏗 Архитектура проекта

```
salary-service/
├── backend/
│   ├── app.py                    # 🎯 Главный файл (3800+ строк)
│   │                             # - Все API endpoints
│   │                             # - Логика загрузки файлов
│   │                             # - Расчёт зарплат
│   │                             # - Генерация отчётов
│   │
│   ├── database.py               # 💾 Работа с БД
│   │                             # - Модели SQLAlchemy
│   │                             # - CRUD операции
│   │                             # - Миграции
│   │
│   ├── auth.py                   # 🔐 OAuth2 Bitrix24
│   │                             # - Авторизация
│   │                             # - Refresh токенов
│   │
│   ├── permissions.py            # 👮 Ролевая модель
│   │                             # - Проверка прав
│   │                             # - Декораторы доступа
│   │
│   ├── api_status.py             # 📊 API статусов периодов
│   │                             # - DRAFT → SENT → PAID
│   │
│   ├── config.py                 # ⚙️ Конфигурация
│   ├── csrf_middleware.py        # 🛡️ CSRF защита
│   │
│   ├── services/
│   │   ├── excel_parser.py       # 📑 Парсинг файлов 1С
│   │   ├── excel_report.py       # 📊 Генерация Excel
│   │   ├── calculation.py        # 🧮 Расчёт зарплат
│   │   ├── geocoding.py          # 🗺️ Yandex Geocoder
│   │   └── yandex_fuel_parser.py # ⛽ Парсинг Яндекс Заправки
│   │
│   └── utils/
│       ├── helpers.py            # 🔧 Вспомогательные функции
│       └── workers.py            # 👷 Нормализация имён
│
├── frontend/
│   ├── templates/
│   │   ├── index.html            # 🏠 Главная (загрузка файлов)
│   │   ├── history.html          # 📚 История периодов
│   │   ├── upload.html           # 📋 Детали загрузки
│   │   ├── comparison.html       # 🔄 Сравнение версий
│   │   ├── search.html           # 🔍 Страница поиска
│   │   └── login.html            # 🔑 Страница входа
│   │
│   └── static/
│       ├── style.css             # 🎨 Стили (Mos-GSM branding)
│       └── security.js           # 🔒 CSRF токены
│
├── docs/
│   ├── BUSINESS_LOGIC.md         # 📖 Бизнес-логика
│   ├── DATABASE_SCHEMA.md        # 💾 Схема БД
│   ├── API_REFERENCE.md          # 🔌 API документация
│   ├── WORKFLOW.md               # 🔄 Воркфлоу
│   └── DEPLOYMENT.md             # 🚀 Развёртывание
│
├── Dockerfile                    # 🐳 Docker конфиг
├── requirements.txt              # 📦 Зависимости
└── README.md                     # 📄 Этот файл
```

---

## 💰 Бизнес-логика расчётов

### Главная формула

```
┌─────────────────────────────────────────────────────────────────┐
│  ИТОГО = Оплата_услуг + Бензин + Транспортные - Яндекс_заправки │
└─────────────────────────────────────────────────────────────────┘
```

### 1️⃣ Оплата услуг

```python
Оплата_услуг = Выручка_от_услуг × (Процент / 100)
```

**Пример:**
- Выручка от услуг: 50 000₽
- Процент: 30%
- Оплата услуг = 50 000 × 0.30 = **15 000₽**

**Особые случаи:**
| Условие | Результат |
|---------|-----------|
| Выезд специалиста > 0 | Оплата услуг = 0 |
| Процент = 0% | Вероятно ошибка в данных |
| Процент = 100% | Особый случай (премия) |

### 2️⃣ Расчёт бензина

**Условия начисления:**
- ✅ Выезд специалиста = 0 (монтажник ехал сам)
- ✅ Адрес указан и валиден
- ❌ Заказ с оплатой клиентом → бензин не начисляется

**Алгоритм:**
```
1. Получить координаты адреса → Yandex Geocoder API
2. Рассчитать расстояние от офиса (Сходненский тупик 16с4)
3. Применить тарифную сетку
```

**Тарифная сетка:**

| Расстояние | Бензин |
|------------|--------|
| до 10 км | 100₽ |
| 10-20 км | 200₽ |
| 20-30 км | 300₽ |
| 30-40 км | 400₽ |
| 40-50 км | 500₽ |
| 50-60 км | 600₽ |
| 60-80 км | 900₽ |
| 80-100 км | 1 200₽ |
| 100-150 км | 1 700₽ |
| 150-200 км | 2 200₽ |
| более 200 км | 3 000₽ |

**Координаты офиса:**
```python
OFFICE_COORDS = (55.8309, 37.4294)  # Москва, Сходненский тупик 16с4
```

### 3️⃣ Расчёт транспортных

**Условия начисления (ВСЕ должны выполняться):**
- ✅ Выручка от услуг > 10 000₽
- ✅ 20% ≤ Процент ≤ 40%
- ❌ Монтажник НЕ на служебном авто

**Сумма:** 1 000₽ фиксировано

**Монтажники на служебном авто** (не получают транспортные):
```python
COMPANY_CAR_WORKERS = [
    "Ветренко Дмитрий Андреевич",
    # ... список
]
```

### 4️⃣ Яндекс Заправки

**Описание:** Компания оплачивает топливо сотрудникам через Яндекс Заправки. В конце месяца сумма вычитается из зарплаты со скидкой 10%.

**Условия применения:**
- ⚠️ Только для периодов **16-30/31** (вторая половина месяца)
- ⚠️ Файл Яндекс Заправки **обязателен** для таких периодов

**Формула вычета:**
```python
Вычет = Сумма_заправок × 0.9  # Монтажник экономит 10%
```

**Пример:**
- Заправился на 10 000₽
- Вычет = 10 000 × 0.9 = **9 000₽** (экономия 1 000₽)

**Определение типа периода:**
```python
def is_second_half_period(period_name: str) -> bool:
    """16-30.11.25 → True, 01-15.11.25 → False"""
    start_day = int(period_name.split("-")[0])
    return start_day >= 16
```

### 5️⃣ Диагностика -50%

Для заказов **с оплатой клиентом** диагностика оплачивается по ставке 50%.

```python
Диагностика_50 = Оплата_диагностики × 0.5
```

### 6️⃣ Заказы с оплатой клиентом

**Определение:** В названии монтажника есть "(оплата клиентом)"

**Особенности:**
| Параметр | Значение |
|----------|----------|
| Бензин | ❌ Не начисляется |
| Транспортные | ❌ Не начисляются |
| Диагностика | 50% от суммы |

---

## 🔄 Workflow статусов

### Диаграмма переходов

```
    ┌──────────┐         ┌──────────┐         ┌──────────┐
    │  DRAFT   │ ──────► │   SENT   │ ──────► │   PAID   │
    │ Черновик │         │ Отправлен │         │  Оплачен │
    └──────────┘         └──────────┘         └──────────┘
         │                    │                    │
         │                    │                    │
         ▼                    ▼                    ▼
    ✏️ Можно               📋 Только             🔒 Только
    редактировать          просмотр              просмотр
```

### Статусы

| Статус | Описание | Разрешённые действия |
|--------|----------|---------------------|
| **DRAFT** | Черновик | Редактирование, удаление, загрузка новых файлов |
| **SENT** | Отправлен монтажникам | Только просмотр |
| **PAID** | Оплачен | Только просмотр |

### Кто может менять статус

| Переход | Роль |
|---------|------|
| DRAFT → SENT | admin, manager |
| SENT → PAID | admin только |
| PAID → SENT | admin только (откат) |
| SENT → DRAFT | admin только (откат) |

---

## 👮 Роли и права доступа

### Матрица прав

| Действие | Admin | Manager | Viewer |
|----------|:-----:|:-------:|:------:|
| Просмотр периодов | ✅ | ✅ | ✅ |
| Загрузка файлов | ✅ | ✅ | ❌ |
| Редактирование данных | ✅ | ✅* | ❌ |
| Удаление периодов | ✅ | ❌ | ❌ |
| Изменение статуса | ✅ | ✅** | ❌ |
| Скачивание отчётов | ✅ | ✅ | ✅ |
| Управление пользователями | ✅ | ❌ | ❌ |

*только в статусе DRAFT
**только DRAFT → SENT

### Авторизация через Bitrix24

```
1. Пользователь нажимает "Войти"
2. Редирект на Bitrix24 OAuth
3. Пользователь авторизуется
4. Bitrix24 возвращает код
5. Сервер обменивает код на токен
6. Создаётся сессия пользователя
```

---

## 💾 База данных

### ER-диаграмма

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   periods   │       │   uploads   │       │   orders    │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │◄──────│ period_id   │◄──────│ upload_id   │
│ name        │       │ id (PK)     │       │ id (PK)     │
│ status      │       │ version     │       │ worker      │
│ month/year  │       │ config_json │       │ order_code  │
└─────────────┘       └─────────────┘       │ address     │
                                            │ revenue_*   │
                                            └──────┬──────┘
                                                   │
                             ┌─────────────────────┤
                             ▼                     ▼
                      ┌─────────────┐       ┌─────────────┐
                      │calculations │       │worker_totals│
                      ├─────────────┤       ├─────────────┤
                      │ order_id    │       │ upload_id   │
                      │ fuel_payment│       │ worker      │
                      │ transport   │       │ total       │
                      │ total       │       └─────────────┘
                      └─────────────┘
```

### Основные таблицы

#### periods — Периоды расчёта
```sql
CREATE TABLE periods (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE,  -- "16-30.11.25"
    month INTEGER NOT NULL,            -- 11
    year INTEGER NOT NULL,             -- 2025
    status VARCHAR(20) DEFAULT 'DRAFT',
    created_at TIMESTAMP DEFAULT NOW(),
    sent_at TIMESTAMP,
    paid_at TIMESTAMP
);
```

#### uploads — Версии загрузок
```sql
CREATE TABLE uploads (
    id SERIAL PRIMARY KEY,
    period_id INTEGER REFERENCES periods(id),
    version INTEGER NOT NULL DEFAULT 1,
    created_at TIMESTAMP DEFAULT NOW(),
    created_by INTEGER REFERENCES users(id),
    config_json JSON  -- {yandex_fuel: {worker: amount}, ...}
);
```

#### orders — Заказы из 1С
```sql
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    upload_id INTEGER REFERENCES uploads(id),
    worker VARCHAR(100) NOT NULL,
    order_code VARCHAR(50),        -- "КАУТ-001405"
    order_date TIMESTAMP,
    address TEXT,
    revenue_total FLOAT,
    revenue_services FLOAT,
    diagnostic FLOAT,
    specialist_fee FLOAT,
    service_payment FLOAT,
    percent VARCHAR(20),           -- "30%"
    is_client_payment BOOLEAN DEFAULT FALSE,
    is_extra_row BOOLEAN DEFAULT FALSE,  -- Ручная корректировка
    manager_comment TEXT
);
```

#### calculations — Результаты расчётов
```sql
CREATE TABLE calculations (
    id SERIAL PRIMARY KEY,
    upload_id INTEGER REFERENCES uploads(id),
    order_id INTEGER REFERENCES orders(id),
    worker VARCHAR(100),
    fuel_payment FLOAT DEFAULT 0,
    transport FLOAT DEFAULT 0,
    diagnostic_50 FLOAT DEFAULT 0,
    total FLOAT DEFAULT 0
);
```

---

## 🔌 API Reference

### Основные endpoints

#### Загрузка файлов
```http
POST /upload
Content-Type: multipart/form-data

files[]: revenue.xlsx
files[]: diagnostic.xlsx
files[]: yandex_fuel.xlsx (опционально)
```

**Response:**
```json
{
  "success": true,
  "period_id": 15,
  "upload_id": 69,
  "workers_count": 12,
  "orders_count": 156
}
```

#### Получение периодов
```http
GET /api/periods
```

**Response:**
```json
{
  "success": true,
  "periods": [
    {
      "id": 15,
      "name": "16-31.12.25",
      "status": "DRAFT",
      "version": 3,
      "workers_count": 12,
      "total": 1250000
    }
  ]
}
```

#### Данные монтажника
```http
GET /api/upload/{upload_id}/worker/{worker_name}
```

**Response:**
```json
{
  "success": true,
  "worker": "Иванов Иван Иванович",
  "orders": [...],
  "totals": {
    "company_total": 45000,
    "client_total": 5000,
    "fuel": 3200,
    "transport": 2000,
    "yandex_fuel": 9000,
    "grand_total": 46200
  }
}
```

#### Обновление ячейки
```http
POST /api/calculation/{calc_id}/update
Content-Type: application/json

{
  "field": "fuel_payment",
  "value": 500
}
```

#### Изменение статуса
```http
POST /api/period/{period_id}/status
Content-Type: application/json

{
  "status": "SENT"
}
```

#### Скачивание отчётов
```http
GET /api/period/{period_id}/download/{archive_type}
```

**archive_type:**
- `all` — все отчёты в ZIP
- `accounting` — только бухгалтерский
- `individual` — только индивидуальные

#### Глобальный поиск
```http
GET /api/search?q=озерная&limit=10
```

**Response:**
```json
{
  "success": true,
  "results": [
    {
      "order_id": 1234,
      "order_code": "КАУТ-001854",
      "order_date": "25.12.25",
      "worker": "Романюк Алексей",
      "address": "КП Агаларов, ул. Озерная, 14",
      "total": 24220,
      "period_name": "16-31.12.25",
      "type": "Компания"
    }
  ]
}
```

---

## 🎨 Фронтенд и UI

### Дизайн-система

**Цвета (Mos-GSM branding):**
```css
--yellow: #F3C04D;        /* Основной акцент */
--yellow-dark: #D4A843;   /* Hover */
--black: #1A1A1A;         /* Текст */
--orange: #E07B3C;        /* Ссылки */
--gray-light: #F5F5F5;    /* Фон */
```

**Шрифты:**
- **Bebas Neue** — заголовки
- **Roboto** — основной текст

### Страницы

#### Главная (`/`)
- Статистика: периоды, суммы
- Форма загрузки файлов (drag & drop)
- Поиск в header
- Последние периоды

#### История (`/history`)
- Список всех периодов
- Фильтр по статусу
- Удаление периодов (admin)

#### Детали загрузки (`/upload/{id}`)
- Список монтажников
- Статистика периода
- Скачивание отчётов
- Изменение статуса

#### Карточка монтажника (`/upload/{id}?worker=...`)
- Таблица заказов
- Редактирование ячеек (inline)
- Итоги и вычеты
- Комментарий менеджера

---

## 🔗 Интеграции

### Bitrix24 OAuth2

**Настройка приложения в Bitrix24:**
1. Маркет → Разработчикам → Создать приложение
2. Тип: Серверное
3. Права: user
4. Redirect URI: `https://your-domain/auth/callback`

**Переменные окружения:**
```env
BITRIX_CLIENT_ID=local.xxxxx
BITRIX_CLIENT_SECRET=xxxxx
BITRIX_DOMAIN=svyaz.bitrix24.ru
```

### Yandex Geocoder

**Получение API ключа:**
1. developer.tech.yandex.ru
2. Создать API-ключ для Геокодера
3. Лимит: 1000 запросов/день (бесплатно)

**Переменные окружения:**
```env
YANDEX_GEOCODER_API_KEY=xxxxx
```

**Кэширование:**
- Результаты геокодирования кэшируются в памяти
- При перезапуске кэш очищается
- Повторные запросы к тому же адресу — из кэша

---

## 🚀 Развёртывание

### Railway (рекомендуется)

```bash
# 1. Fork репозитория на GitHub

# 2. Создать проект в Railway
railway login
railway init

# 3. Добавить PostgreSQL
railway add postgresql

# 4. Установить переменные окружения
railway variables set DATABASE_URL=...
railway variables set BITRIX_CLIENT_ID=...
railway variables set BITRIX_CLIENT_SECRET=...
railway variables set YANDEX_GEOCODER_API_KEY=...

# 5. Деплой
railway up
```

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "backend.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

```bash
docker build -t salary-service .
docker run -p 8000:8000 --env-file .env salary-service
```

### Локальная разработка

```bash
# 1. Клонировать
git clone https://github.com/your-repo/salary-service.git
cd salary-service

# 2. Виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 3. Зависимости
pip install -r requirements.txt

# 4. Переменные окружения
cp .env.example .env
# Заполнить .env

# 5. Запуск
uvicorn backend.app:app --reload --port 8000
```

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `DATABASE_URL` | PostgreSQL connection string | `postgresql://user:pass@host:5432/db` |
| `BITRIX_CLIENT_ID` | OAuth Client ID | `local.xxxxx` |
| `BITRIX_CLIENT_SECRET` | OAuth Secret | `xxxxx` |
| `BITRIX_DOMAIN` | Домен Bitrix24 | `svyaz.bitrix24.ru` |
| `YANDEX_GEOCODER_API_KEY` | API ключ геокодера | `xxxxx` |
| `SECRET_KEY` | Ключ для сессий | `random-string` |

### Конфигурация расчётов

```python
# backend/config.py

DEFAULT_CONFIG = {
    # Транспортные
    "transport_min_revenue": 10000,  # Минимальная выручка
    "transport_percent_min": 20,     # Минимальный процент
    "transport_percent_max": 40,     # Максимальный процент
    "transport_amount": 1000,        # Сумма

    # Яндекс Заправки
    "yandex_fuel_discount": 0.9,     # 90% от суммы

    # Диагностика
    "diagnostic_client_rate": 0.5,   # 50% для клиента
}

# Монтажники на служебном авто
COMPANY_CAR_WORKERS = [
    "Ветренко Дмитрий Андреевич",
]

# Координаты офиса
OFFICE_COORDS = (55.8309, 37.4294)

# Тарифы на бензин
FUEL_TARIFFS = [
    (10, 100),
    (20, 200),
    # ...
]
```

---

## 🔧 Troubleshooting

### Проблема: Бензин не рассчитывается

**Причины:**
1. Адрес не распознан Yandex Geocoder
2. Выезд специалиста > 0
3. Заказ с оплатой клиентом

**Решение:**
```python
# Проверить в логах
print(f"Address: {address}, Geocoded: {coords}")
```

### Проблема: Яндекс Заправки не вычитаются

**Причины:**
1. Период 01-15 (первая половина)
2. Не загружен файл Яндекс Заправки
3. Имя монтажника не совпадает

**Решение:**
- Проверить что период 16-30/31
- Проверить наличие файла в config_json
- Нормализовать имена

### Проблема: Поиск не находит результаты

**Причины:**
1. pg_trgm не установлен
2. Кириллица не обрабатывается

**Решение:**
```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

### Проблема: OAuth не работает

**Причины:**
1. Неверный redirect_uri
2. Протухший токен

**Решение:**
- Проверить что redirect_uri совпадает с настройками приложения
- Использовать https://

---

## 📞 Контакты

**Mos-GSM** — услуги связи и мониторинга

- 🌐 Website: mos-gsm.ru
- 📧 Email: info@mos-gsm.ru

---

## 📜 Лицензия

Proprietary. Все права защищены. © Mos-GSM 2024-2026
