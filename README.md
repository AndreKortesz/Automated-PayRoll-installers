# 📊 Сервис расчёта зарплаты монтажников

Веб-сервис для автоматического расчёта зарплаты монтажников на основе Excel-файлов из 1С.

## 🚀 Возможности

- Загрузка двух Excel-файлов (объекты до 10000₽ и более 10000₽)
- Автоматический расчёт:
  - **Оплата бензина** — расстояние туда-обратно × коэффициент (с учётом количества дней)
  - **Транспортные** — для монтажей от 10000₽ с комиссией 20-40%
  - **Диагностика -50%** — для строк "оплата клиентом"
  - **Итого** — сумма всех начислений
- Система предупреждений (Alarms) для ручной проверки
- Настраиваемые параметры расчёта
- Добавление дополнительных строк (эвакуатор, компенсации и т.д.)
- Генерация ZIP-архива с общим отчётом и индивидуальными файлами

## 📋 Требования

- Python 3.11+
- API-ключ Яндекс.Геокодер (для расчёта расстояний)

## 🛠 Локальная установка

```bash
# Клонировать репозиторий
git clone <repo-url>
cd salary-service

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
cd backend
uvicorn app:app --reload --port 8000
```

Открыть в браузере: http://localhost:8000

## 🚂 Деплой на Railway

1. Создать проект на [Railway](https://railway.app)
2. Подключить GitHub репозиторий
3. Railway автоматически определит Dockerfile и задеплоит

## 📁 Структура проекта

```
salary-service/
├── backend/
│   └── app.py          # FastAPI приложение
├── frontend/
│   ├── templates/
│   │   └── index.html  # Веб-интерфейс
│   └── static/         # Статические файлы
├── Dockerfile
├── requirements.txt
└── README.md
```

## ⚙️ Настройки (по умолчанию)

| Параметр | Значение | Описание |
|----------|----------|----------|
| Базовый адрес | Москва, Сходненский тупик 16с4 | Точка А для расчёта расстояния |
| Коэффициент бензина | 7 | Множитель для расчёта стоимости |
| Максимум за бензин | 3000₽ | Ограничение на оплату бензина |
| Транспортные | 1000₽ | Фиксированная сумма |
| Мин. выручка для транспортных | 10000₽ | Порог для начисления транспортных |
| Процент комиссии | 20-40% | Диапазон для начисления транспортных |
| Процент диагностики | 50% | Вычет из "Диагностика" |

## ⚠️ Система предупреждений

Сервис автоматически выводит предупреждения для:

- `Сумма оплаты от услуг` > 20 000₽
- `Процент от выручки` ≠ 30%, 50%, 100%
- `Выручка (выезд) специалиста` > 3 500₽

## 📝 Формат выходных файлов

```
Зарплата_01-15.11.25.zip
├── Общий_отчет 01-15.11.25.xlsx
├── Ветренко 01-15.11.25.xlsx
├── Викулин 01-15.11.25.xlsx
├── Гуднев 01-15.11.25.xlsx
├── Романюк 01-15.11.25.xlsx
├── Старков 01-15.11.25.xlsx
└── Фадин 01-15.11.25.xlsx
```

## 🔧 API Endpoints

- `GET /` — Веб-интерфейс
- `POST /upload` — Загрузка Excel-файлов
- `POST /calculate` — Расчёт зарплат
- `GET /download/{session_id}` — Скачивание результата

## 📜 Лицензия

Для внутреннего использования.
